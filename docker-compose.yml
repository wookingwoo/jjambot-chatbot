services:
  jjambot:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
    networks:
      - wookingwoo-op-bridge

networks:
  wookingwoo-op-bridge:
    external: true
