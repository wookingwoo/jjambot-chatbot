PORT=8000

# Supabase project → Settings → API
SUPABASE_URL=https://cvhcbjbkzontrgiesqcb.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Shared secret Kakao must pass back as a `?token=` query param on every
# skill URL (Kakao can't send custom headers). Generate with:
#   openssl rand -hex 32
SKILL_SECRET=
