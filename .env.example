PORT=8000

# Supabase project → Settings → API
SUPABASE_URL=https://cvhcbjbkzontrgiesqcb.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Shared secret Kakao must send back as the `X-Skill-Secret` header on every
# skill request (set via the "헤더 이름"/"헤더 값" fields when registering the
# skill URL in OpenBuilder). Generate with:
#   openssl rand -hex 32
SKILL_SECRET=
